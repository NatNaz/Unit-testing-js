//A simple class containing methods for calculate numbers
class calculator {
  //Creates an instance of calculator.

  constructor() {}

  /*@param {Number} a
   *@param {Number} b
   *@return {Number} sum of 2 numbers
   *@memberof calculator
   */
  add(a, b) {
    if ((typeof a) || (typeof b)) !== "number") {
      throw new Error(
        `Cannot count the sum as at least one of the arguments is not of type "Number"`
      );
    } else {
      let sum = a + b;
      return sum;
    }
  }

  /*@param {Number} a
   *@param {Number} b
   *@return {Number} product of arguments
   *@memberof calculator
   */
  multiply(a, b) {
    if (typeof (a || b) !== "number") {
      throw new Error(
        `Cannot count the product as at least one of the arguments is not of type "Number"`
      );
    } else {
      let product = a * b;
      return product;
    }
  }
}

module.exports = calculator;
